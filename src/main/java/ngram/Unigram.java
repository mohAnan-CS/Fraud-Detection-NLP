package ngram;

import java.util.ArrayList;

public class Unigram {

    public static final ArrayList<Model> arrayListUniGram = new ArrayList<>();


/*
     public static ArrayList<Model> getUniGram(ArrayList<ArrayList<String>> arrayListsWord){


         for (int i = 0; i < arrayListsWord.size(); i++) {

             for (int j = 0; j < arrayListsWord.get(i).size(); j++) {

                 Model modelObj = new Model();
                 modelObj.setWord(arrayListsWord.get(i).get(j));
                 modelObj.setGram(1);
                 modelObj.setCount(1);
                 System.out.println(modelObj.getWord());
                 addWord(arrayListUniGram , modelObj);

             }

         }

         return arrayListUniGram;

     }
*/


}
